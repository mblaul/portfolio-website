import React from 'react';
import styled from 'styled-components';

const AboutStyles = styled.div`color: purple;`;

const About = () => {
  return (
    <AboutStyles>
      <p>◀ That's me over there</p>
    </AboutStyles>
  );
};

export default About;
